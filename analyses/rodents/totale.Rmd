---
title: "totale"
output: github_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

library(soar)
library(ggplot2)
library(dplyr)
theme_set(theme_bw())
library(gratia)
load_mgcv()

```

```{r}

tms <- get_treatment_means()

ctrls <- filter(tms, oplottype == "CC")

ggplot(ctrls, aes(censusdate, total_e_ma)) +
  geom_line()

```